{
  "version": 2,
  "builds": [
    { "src": "admin/app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/admin/(.*)", "dest": "/admin/app.py" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/projects/(.*)", "dest": "/public/projects/$1" }
  ]
}